name: Swift

on:
  push:
    tags: ["*"]
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  test-substate-macos-10-15:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test
        run: xcodebuild test -scheme Substate -destination 'platform=macOS' | xcpretty
  test-counter-ios-14-0:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test
        run: xcodebuild test -scheme 'Counter (iOS)' -destination 'platform=iOS Simulator,OS=14.4,name=iPhone 12 mini' | xcpretty
